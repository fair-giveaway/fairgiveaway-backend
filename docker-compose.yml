services:
  backend:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: fairgiveaway-backend
    ports:
      - "7860:7860"
    env_file:
      - .env
    restart: unless-stopped
